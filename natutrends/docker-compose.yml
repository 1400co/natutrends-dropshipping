version: '4'
services:
  web:
    container_name: natutrends_web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:80"
    volumes:
      - /app/node_modules
      - .:/app
