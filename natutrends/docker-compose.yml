version: '4'
services:
  web:
    container_name: digital_experts_web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:80"
    volumes:
      - /app/node_modules
      - .:/app
